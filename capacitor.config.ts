import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.trusttecnologia.app',
  appName: 'Trust Tecnologia',
  webDir: 'dist'
};

export default config;
